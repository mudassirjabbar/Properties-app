import 'package:flutter/material.dart';

class WhilistScreen extends StatelessWidget {
  const WhilistScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
