import 'package:flutter/material.dart';

class WhilistNavpage extends StatelessWidget {
  const WhilistNavpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
