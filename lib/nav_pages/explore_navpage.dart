import 'package:flutter/material.dart';

class ExploreNavpage extends StatelessWidget {
  const ExploreNavpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
