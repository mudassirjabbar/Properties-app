import 'package:flutter/material.dart';

class AccountNavpage extends StatelessWidget {
  const AccountNavpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
