import 'package:flutter/material.dart';

class ListingNavpage extends StatelessWidget {
  const ListingNavpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
